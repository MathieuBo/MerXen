name: merxen
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      # Paths are relative to this file's directory: conda runs pip with its
      # working directory set to the environment file's location.
      - -e "../[dev]"
